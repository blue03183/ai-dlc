{
  "name": "table-order",
  "version": "1.0.0",
  "private": true,
  "description": "테이블오더 서비스 모노레포",
  "scripts": {
    "backend:dev": "npm --prefix backend run start:dev",
    "customer:dev": "npm --prefix customer-app run dev",
    "admin:dev": "npm --prefix admin-app run dev"
  }
}
